title: Muneer Khan
description: Digital Portfolio & Blog

markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

defaults:
  - scope: 
      path: "blogs"
    values: 
      layout: "blog-post"
  
exclude:
  - Gemfile
  - Gemfile.lock
  - vendor
